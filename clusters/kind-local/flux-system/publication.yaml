apiVersion: postgresql.cnpg.io/v1
kind: Publication
metadata:
  name: recruitment-pub
  namespace: hrteam  
spec:
  cluster:
    name: recruitment
  dbname: app
  name: recruitment_publication
  target:
    allTables: true
